<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false">
    <property name="LOGS" value="./log"/>
    <property name="FILENAME" value="alcm"/>

    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
            <pattern>
                [%d{ISO8601}]-[%15.15thread]-[%-15.-15X{sessionId}]-[%-5level]-[%-30.-30logger{0}]-[%msg]%n
            </pattern>
        </layout>
    </appender>

    <appender name="RollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOGS}/${FILENAME}.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <Pattern>%d{ISO8601} %p [%t] %C{1.}: %msg%n%throwable</Pattern>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- rollover daily and when the file reaches 5MB and have backup up to 5 -->
            <fileNamePattern>${LOGS}/${FILENAME}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>5MB</maxFileSize>
            <totalSizeCap>25MB</totalSizeCap>
            <maxHistory>5</maxHistory>
        </rollingPolicy>
    </appender>

    <appender name="ASYNC" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="Console"/>
        <appender-ref ref="RollingFile"/>
    </appender>

    <logger name="de.arvato" level="INFO" additivity="false">
        <appender-ref ref="Console"/>
        <appender-ref ref="RollingFile"/>
    </logger>

    <root level="ERROR">
        <appender-ref ref="Console"/>
        <appender-ref ref="RollingFile"/>
    </root>

</configuration>
